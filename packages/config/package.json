{
  "name": "@scout/config",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "echo \"No lint configured yet\" && exit 0",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
