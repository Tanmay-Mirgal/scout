/**
 * @scout/config
 *
 * Shared configuration for the SCOUT monorepo.
 * Intended for shared tool configs (ESLint, TypeScript base config, etc.)
 * and any cross-application constants.
 */

export const DEFAULT_API_PORT = 4000;
export const DEFAULT_WEB_PORT = 3000;
