/**
 * @scout/shared
 *
 * Shared types, constants, and utilities used across apps/web and apps/api.
 * Add shared interfaces, enums, and helpers here as the project grows.
 */

// Package version — kept in sync with the root monorepo version.
export const SCOUT_VERSION = "0.1.0";
